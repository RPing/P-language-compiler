/**
 * relation2.p: < <= = >= > <>       simple case(s), real
 */
//&T-

relation2;

begin

        var a: boolean;
        var b, c: real;

        a := 10.1 < 2.33;
        a := b = 0.0;
        a := c >= b;
        a := 1.23E-1 > b;
        a := 55.66 <= c;
end
end relation2

